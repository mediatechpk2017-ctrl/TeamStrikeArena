Updated dependency versions to compatible releases: React 18, Three 0.157, @react-three/fiber, @react-three/drei, @dimforge/rapier3d-compat; updated server TypeScript tooling. Ensures npm install and builds succeed across client and server.
